// @flow
// https://www.htmlcsscolor.com
const dodgerBlue = '#008AE5'
const dodgerBlue30 = 'rgba(0, 138, 229, 0.70)'
const deepSkyBlue = '#0DCCFF'
// eslint-disable-next-line no-unused-vars
const razzmatazz = '#FF0073'
// eslint-disable-next-line no-unused-vars
const carrotOrange = '#F7931E'
// eslint-disable-next-line no-unused-vars
const lemon = '#FFF022'
const haiti = '#24222B'
const black = '#000000'
const nightRider = '#333333'
const grey = '#888888'
const noble = '#999999'
const white = '#FFFFFF'
const ghostWhite = '#F8F8FF'

// Uses
const brandPrimary = dodgerBlue
const brandPrimary30 = dodgerBlue30
const brandSecondary = deepSkyBlue
const background = ghostWhite
const backgroundDark = haiti
const border = ghostWhite
const textDark = black
const textDefault = nightRider
const textLight = ghostWhite
const icons = grey
const gray = noble

// Color use
const colors: {
  background: string,
  backgroundDark: string,
  black: string,
  border: string,
  brandPrimary: string,
  brandPrimary30: string,
  brandSecondary: string,
  gray: string,
  icons: string,
  link: string,
  textDark: string,
  textDefault: string,
  textLight: string,
  white: string,
} = {
  background,
  backgroundDark,
  black,
  border,
  brandPrimary,
  brandPrimary30,
  brandSecondary,
  gray,
  icons,
  link: brandPrimary,
  textDark,
  textDefault,
  textLight,
  white,
}

export default colors
